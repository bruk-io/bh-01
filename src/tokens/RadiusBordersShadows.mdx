import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Tokens/Primitives/Radius, Borders & Shadows" />

# Radius

<div style={{ display: 'flex', gap: '1.5rem', flexWrap: 'wrap', marginBottom: '2rem' }}>
  {[
    ['none', '0'],
    ['sm', '0.125rem'],
    ['md', '0.375rem'],
    ['lg', '0.5rem'],
    ['xl', '0.75rem'],
    ['2xl', '1rem'],
    ['full', '9999px'],
  ].map(([key, val]) => (
    <div key={key} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '0.5rem' }}>
      <div style={{ width: '4rem', height: '4rem', background: 'var(--bh-color-primary)', borderRadius: `var(--bh-radius-${key})` }} />
      <code style={{ fontSize: '0.7rem' }}>--bh-radius-{key}</code>
      <span style={{ fontSize: '0.65rem', color: 'var(--bh-color-text-muted)' }}>{val}</span>
    </div>
  ))}
</div>

# Borders

<div style={{ display: 'flex', gap: '2rem', flexWrap: 'wrap', marginBottom: '2rem' }}>
  {[
    ['0', '0px'],
    ['1', '1px'],
    ['2', '2px'],
    ['4', '4px'],
  ].map(([key, val]) => (
    <div key={key} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '0.5rem' }}>
      <div style={{ width: '4rem', height: '4rem', borderRadius: '0.375rem', border: `var(--bh-border-${key}) solid var(--bh-color-text)`, background: 'var(--bh-color-surface-raised)' }} />
      <code style={{ fontSize: '0.7rem' }}>--bh-border-{key}</code>
      <span style={{ fontSize: '0.65rem', color: 'var(--bh-color-text-muted)' }}>{val}</span>
    </div>
  ))}
</div>

# Shadows

<div style={{ display: 'flex', gap: '2rem', flexWrap: 'wrap' }}>
  {[
    ['none', 'none'],
    ['sm', 'Subtle'],
    ['md', 'Medium'],
    ['lg', 'Large'],
    ['xl', 'Extra large'],
  ].map(([key, desc]) => (
    <div key={key} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '0.5rem' }}>
      <div style={{ width: '6rem', height: '4rem', borderRadius: '0.5rem', background: 'var(--bh-color-surface-raised)', boxShadow: `var(--bh-shadow-${key})` }} />
      <code style={{ fontSize: '0.7rem' }}>--bh-shadow-{key}</code>
      <span style={{ fontSize: '0.65rem', color: 'var(--bh-color-text-muted)' }}>{desc}</span>
    </div>
  ))}
</div>
