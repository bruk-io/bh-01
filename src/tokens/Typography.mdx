import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Tokens/Primitives/Typography" />

# Typography

Font families, sizes, weights, and line heights.

## Font Families

<div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem' }}>
  <div>
    <code style={{ fontSize: '0.75rem', display: 'block', marginBottom: '0.25rem' }}>--bh-font-sans</code>
    <span style={{ fontFamily: 'var(--bh-font-sans)', fontSize: '1.25rem' }}>The quick brown fox jumps over the lazy dog</span>
  </div>
  <div>
    <code style={{ fontSize: '0.75rem', display: 'block', marginBottom: '0.25rem' }}>--bh-font-mono</code>
    <span style={{ fontFamily: 'var(--bh-font-mono)', fontSize: '1.25rem' }}>The quick brown fox jumps over the lazy dog</span>
  </div>
</div>

## Font Sizes

<div style={{ display: 'flex', flexDirection: 'column', gap: '1rem', marginTop: '1rem' }}>
  {[
    ['xs', '0.75rem (12px)'],
    ['sm', '0.875rem (14px)'],
    ['base', '1rem (16px)'],
    ['lg', '1.125rem (18px)'],
    ['xl', '1.25rem (20px)'],
    ['2xl', '1.5rem (24px)'],
    ['3xl', '1.875rem (30px)'],
    ['4xl', '2.25rem (36px)'],
  ].map(([key, desc]) => (
    <div key={key} style={{ display: 'flex', alignItems: 'baseline', gap: '1rem' }}>
      <code style={{ fontSize: '0.75rem', width: '10rem', flexShrink: 0 }}>--bh-text-{key}</code>
      <span style={{ fontSize: `var(--bh-text-${key})`, fontFamily: 'var(--bh-font-sans)' }}>Aa</span>
      <span style={{ fontSize: '0.75rem', color: 'var(--bh-color-text-muted)' }}>{desc}</span>
    </div>
  ))}
</div>

## Font Weights

<div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem', marginTop: '1rem' }}>
  {[
    ['normal', '400'],
    ['medium', '500'],
    ['semibold', '600'],
    ['bold', '700'],
  ].map(([key, val]) => (
    <div key={key} style={{ display: 'flex', alignItems: 'baseline', gap: '1rem' }}>
      <code style={{ fontSize: '0.75rem', width: '14rem', flexShrink: 0 }}>--bh-font-{key}</code>
      <span style={{ fontWeight: `var(--bh-font-${key})`, fontFamily: 'var(--bh-font-sans)', fontSize: '1.125rem' }}>The quick brown fox ({val})</span>
    </div>
  ))}
</div>

## Line Heights

<div style={{ display: 'flex', flexDirection: 'column', gap: '1rem', marginTop: '1rem' }}>
  {[
    ['none', '1'],
    ['tight', '1.25'],
    ['snug', '1.375'],
    ['normal', '1.5'],
    ['relaxed', '1.625'],
  ].map(([key, val]) => (
    <div key={key} style={{ display: 'flex', gap: '1rem' }}>
      <code style={{ fontSize: '0.75rem', width: '14rem', flexShrink: 0, paddingTop: '0.25rem' }}>--bh-leading-{key}</code>
      <div style={{ lineHeight: `var(--bh-leading-${key})`, fontFamily: 'var(--bh-font-sans)', fontSize: '0.875rem', maxWidth: '300px', background: 'var(--bh-color-surface)', padding: '0.5rem', borderRadius: '0.25rem' }}>
        This text demonstrates the <strong>{key}</strong> line height ({val}). Multiple lines show the spacing between them.
      </div>
    </div>
  ))}
</div>
